import { Channel } from '@mycelium-now/core';
/**
 * Publish an event.
 *
 * @param channel Mycelium channel to publish on
 */
export declare function usePublishEvent(channel: Channel | undefined): {
    publish: <TData = unknown>(event: string, data: TData, includePublisher?: boolean) => Promise<void>;
    isLoading: boolean;
    isSuccess: boolean;
    isError: boolean;
    error: Error | null;
};
